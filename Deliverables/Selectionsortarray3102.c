#include <stdio.h>
#include <stdlib.h>

int inputofarray(n) { // n is the number of elements in the list}
  
  for (int j = 0; j< n-1; j++ ){                // counter to take values of each value of input array 
    scanf (%d, &arr[j])

    return 0;
}
}

int swap(min, arr[]) {                         // swap func to swap the min element with the arr[i] position its being compared to
    temp = arr[i];
    arr [i] = min;
    min = temp;
}
int SelectionSort(n){
     int n;                                    // initialize number
     printf('Enter size of array');
     scanf(%d, n);                             // Inputs value of number n 
     arr = (array) malloc (sizeof(n));        // setting out space for the array in memory
    inputofarray(n)                           // call func to collect input of the array                        
     min = arr[0];                           // initialize min val to location 0
  for (int i = 1; i< n-i; i++ ){             // basically looping 
    if (min > arr [i]){                      // if the min element is not min while comparing
        swap(min,arr[i]);                   // call the swap function to swap those and then continue 
    }
        min = arr[i];                       //  Move min up 1 element 
    }
  
     return 0;
    }

  
                                
                   
   